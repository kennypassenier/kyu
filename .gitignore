# Build output
/target/
/target-trixie/

# Local secrets and scratch — never committed (standing rule 10)
.env
.env.*
!.env.example
/dist/

# Editor noise
*.swp
.idea/
.vscode/

# --- project additions below (kept by chassis sync) ---
# kyu keeps its SQLite store and backups out of git.
/data
*.db
*.db-wal
*.db-shm
kyu.pre-v*.db
